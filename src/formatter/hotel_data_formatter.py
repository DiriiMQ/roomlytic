from src.models.hotel_data_model import HotelDataModel

class HotelDataFormatter:
    def filter_and_format(self, hotel_data: list[HotelDataModel], hotel_ids: list[str], destination_ids: list[str]) -> list[HotelDataModel]:
        # Logic to filter and format data
        pass