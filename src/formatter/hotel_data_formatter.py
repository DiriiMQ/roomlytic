from src.models.hotel_data_model import Hotel

class HotelDataFormatter:
    def filter_and_format(self, hotel_data: list[Hotel], hotel_ids: list[str], destination_ids: list[str]) -> list[Hotel]:
        # Logic to filter and format data
        pass